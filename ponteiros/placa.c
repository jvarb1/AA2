#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "placa.h"

Placa* criarPlaca(char letra[4], char codigo[5]);{
  Placa *c = (placa*)malloc(sizeof(Placa));

  c -> for(int i=0; i<n; i++){
    
    Placa* placa = criarPlaca(letras, cod);
    placas[i] = placa;
  }
  for(int i=0; i<n; i++){
    int p = i+1;
    printf("\n%d° placa: %s-%s \n", p, placas[i]->codigo, placas[i]->letras);
  }
}

return plc;
}